package pack01_base;

public class Ex04_Car {
	//해당하는 Car클래스는 속성 gas를 가진다.
	//gas는 숫자형태로 속성이 관리가 된다.(Ex04_Car는 gas를 무조건 입력받아야지만 생성이 가능한 클래스이다.)
	
	int gas;

	public Ex04_Car(int gas) {
		this.gas = gas;
	}
	
	//※is, has로 끝나는 것 <-boolean 타입을 리턴하는 메소드일 확률 98%
	//isLeftGas라는 메소드를 구현해보기
	//해당하는 메소드는 true 또는 false의 결과만 리턴하는 메소드이다
	//true, false의 판단은 gas가 0인 경우 false와 gas가 없습니다라는 메세지가 콘솔에 출력이되고,
	//그게 아니라면 true와 gas가 있습니다라는 메세지가 콘솔에 출력이 된다.
	
	boolean isLeftGas() {
		if(gas==0) {
			System.out.println("gas가 없습니다.");
			return false;
		}else {
			System.out.println("gas가 있습니다.");
			return true;
		}
//		return true;
	}
	
	
	
	
}
